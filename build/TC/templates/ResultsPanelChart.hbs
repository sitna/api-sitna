<div class="tc-track-chart">
    {{#if msg}}
        {{msg}}
    {{else}}
        <span id="elevationGain" >{{i18n "geo.trk.chart.elevationGain"}}: +{{upHill}}m, -{{downHill}}m</span>
        <div class="tc-chart"></div>
    {{/if}}
</div>
