<h1>Clase {{name}}</h1>
<div class="box meta">
    {{#if uses}}
        <div class="uses">
            Utiliza
            <ul class="inline commas">
                {{#uses}}
                    <li><a href="{{.}}.html">{{.}}</a></li>
                {{/uses}}
            </ul>
        </div>
    {{/if}}

    {{#if extends}}
        <div class="extends">
            Extiende {{#crossLink extends}}{{/crossLink}}
        </div>
    {{/if}}

    {{#if foundAt}}
        <div class="foundat" style="display:none">
            Se define en: <a href="{{foundAt}}">`{{{file}}}:{{{line}}}`</a>
        </div>
    {{/if}}

    {{#if module}}
        {{#if submodule}}
            Sección relacionada: {{#crossLinkModule submodule}}{{/crossLinkModule}}<br>
            Sección principal: {{#crossLinkModule module}}{{/crossLinkModule}}
        {{else}}
            Sección relacionada: {{#crossLinkModule module}}{{/crossLinkModule}}
        {{/if}}
    {{/if}}

    {{#if since}}
        <p>Disponible desde {{since}}</p>
    {{/if}}
</div>

{{#if deprecated}}
    <div class="box deprecated">
        <p>
        {{#if deprecationMessage}}
            <strong>Deprecated:</strong> {{deprecationMessage}}
        {{else}}
            Esta clase está "deprecated".
        {{/if}}
        </p>
    </div>
{{/if}}

<div class="box intro">
    {{{classDescription}}}
</div>

<!-- Class member index -->

<a name="index" class="anchor-link"></a>
<div class="index">
<h2>Índice</h2>

{{>options}}

{{#if properties}}
    <div class="index-section properties">
        <h3>Propiedades</h3>

        <ul class="index-list properties{{#if extends}} extends{{/if}}">
            {{#properties}}
                <li class="index-item property{{#if access}} {{access}}{{/if}}{{#if deprecated}} deprecated{{/if}}{{#if overwritten_from}} inherited{{/if}}{{#if extended_from}} inherited{{/if}}">
                    <a href="#property_{{name}}">{{name}}</a>

                    {{#if static}}
                        <span class="flag static">static</span>
                    {{/if}}
                </li>
            {{/properties}}
        </ul>
    </div>
{{/if}}

{{#if attrs}}
    <div class="index-section attrs">
        <h3>Atributos</h3>

        <ul class="index-list attrs{{#if extends}} extends{{/if}}">
            {{#attrs}}
                <li class="index-item attr{{#if access}} {{access}}{{/if}}{{#if deprecated}} deprecated{{/if}}{{#if overwritten_from}} inherited{{/if}}{{#if extended_from}} inherited{{/if}}">
                    <a href="#attr_{{name}}">{{name}}</a>
                </li>
            {{/attrs}}
        </ul>
    </div>
{{/if}}


{{#if methods}}
    <div class="index-section methods">
        <h3>Métodos</h3>

        <ul class="index-list methods{{#if extends}} extends{{/if}}">

            {{#is_constructor}}
                <li class="index-item method{{#if access}} {{access}}{{/if}}{{#if deprecated}} deprecated{{/if}} constructor">
                    <a href="#method_{{name}}">{{name}}</a>

                    <span class="flag constructor">constructor</span>

                    {{#if static}}
                        <span class="flag static">static</span>
                    {{/if}}
                    {{#if deprecated}}
                        <span class="flag deprecated">deprecated</span>
                    {{/if}}
                </li>
            {{/is_constructor}}

            {{#methods}}
                <li class="index-item method{{#if access}} {{access}}{{/if}}{{#if deprecated}} deprecated{{/if}}{{#if overwritten_from}} inherited{{/if}}{{#if extended_from}} inherited{{/if}}">
                    <a href="#method_{{name}}">{{name}}</a>

                    {{#if static}}
                        <span class="flag static">static</span>
                    {{/if}}
                    {{#if deprecated}}
                        <span class="flag deprecated">deprecated</span>
                    {{/if}}
                </li>
            {{/methods}}
        </ul>
    </div>
{{/if}}

{{#if events}}
        <div class="index-section events">
            <h3>Eventos</h3>

            <ul class="index-list events{{#if extends}} extends{{/if}}">
                {{#events}}
                    <li class="index-item event{{#if access}} {{access}}{{/if}}{{#if deprecated}} deprecated{{/if}}{{#if overwritten_from}} inherited{{/if}}{{#if extended_from}} inherited{{/if}}">
                        <a href="#event_{{name}}">{{name}}</a>

                        {{#if static}}
                            <span class="flag static">static</span>
                        {{/if}}
                        {{#if deprecated}}
                            <span class="flag deprecated">deprecated</span>
                        {{/if}}
                    </li>
                {{/events}}
            </ul>
        </div>
    {{/if}}
</div>


<div class="class-detail">

{{#if properties}}
    <div class="properties-detail">
        <h2>Propiedades</h2>

        {{#properties}}
            {{>props}}
        {{/properties}}

        <div class="no-visible-items-message">
            <p>No hay propiedades que cumplan la configuración actual de filtro. Puede cambiar la configuración de filtro en la sección "Índice" de esta página. <a href="#index" class="index-jump-link">índice</a></p>
        </div>

    </div>
{{/if}}

{{#if attrs}}
    <div class="attrs-detail">
        <h2>Atributos</h2>

        {{#attrs}}
            {{>attrs}}
        {{/attrs}}
    </div>
{{/if}}

{{#is_constructor}}
    <div class="constructor">
        <h2>Constructor</h2>
        {{>method}}
    </div>
{{/is_constructor}}

{{#if methods}}
    <div class="methods-detail">
        <h2>Métodos</h2>

        {{#methods}}
            {{>method}}
        {{/methods}}

        <div class="no-visible-items-message">
            <p>No hay métodos que cumplan la configuración actual de filtro. Puede cambiar la configuración de filtro en la sección "Índice" de esta página. <a href="#index" class="index-jump-link">índice</a></p>
        </div>

    </div>
{{/if}}

{{#if events}}
    <div class="events-detail">
        <h2>Eventos</h2>

        {{#events}}
            {{>events}}
        {{/events}}

        <div class="no-visible-items-message">
            <p>No hay eventos que cumplan la configuración actual de filtro. Puede cambiar la configuración de filtro en la sección "Índice" de esta página. <a href="#index" class="index-jump-link">índice</a></p>
        </div>

    </div>
{{/if}}

</div>

