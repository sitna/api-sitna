<h2>{{title}}</h2>
<div class="tc-ctl-tctr-select">
    <form>
        {{#each controls~}}
        <label class="tc-ctl-tctr-tab tc-ctl-tctr-tab-{{@index}}" style="width:calc(100%/{{../count}} - 1px)"><input type="radio" name="sctnr-sel" value="{{@index}}" /><span>{{title}}</span></label>
        {{~/each}}
    </form>
</div>
{{#each controls}}
<div class="tc-ctl-tctr-elm tc-ctl-tctr-elm-{{@index}} tc-hidden">
</div>
{{/each}}